<atlassian-plugin key="${atlassian.plugin.key}" name="${project.name}" plugins-version="2">
    <plugin-info>
        <description>${project.description}</description>
        <version>${project.version}</version>
        <vendor name="${project.organization.name}" url="${project.organization.url}"/>
        <param name="plugin-icon">images/pluginIcon.png</param>
        <param name="plugin-logo">images/pluginLogo.png</param>
    </plugin-info>

    <rest name="Divide" key="testApiRest" path="/myapi" version="1">
        <description>Provides necessary API methods for Divide Issue plugin.</description>
    </rest>

    <ao key="ao-entities">
        <description>The module configuring the Divide Issue template Active Objects</description>
        <entity>ru.alfabank.bcm.optionmanagment.model.Product</entity>
    </ao>

</atlassian-plugin>
